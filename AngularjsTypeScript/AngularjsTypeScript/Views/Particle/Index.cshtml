@{
    ViewBag.Title = "Index";
}

@section head {
    <script src="@Url.Content("~/Scripts/lib/angular.js")"></script>
    <script src="@Url.Content("~/Scripts/app/ParticleCtrl.js")"></script>
}

<div ng-app>
    <div ng-controller="ParticleApp.ParticleCtrl">

        <p>{{title}}</p>

      <ul>
        <li ng-repeat="particle in particles">
          <p>{{particle.name}} ({{particle.symbol}}<sub>{{particle.subsymbol}}</sub>)</p>
        </li>
      </ul>

      <p>Total number of particles: <span class="label label-info">{{particles.length}}</span></p>

    </div>

    <!-- Experiments -->

    @*<table>
        <tr><th>row number</th></tr>
        <tr ng-repeat="i in [0, 1, 2, 3, 4, 5, 6, 7]"><td>{{i+1}}</td></tr>
    </table>*@

</div>