@{
    ViewBag.Title = "Collections";
}

@section header{
    <script src="@Url.Content("~/Scripts/KnockuotTypescript/003-collections.js")" type="text/javascript"></script>
}

<p>Your seat reservations <span class="badge badge-success" data-bind="text: seats().length"></span></p>

<h3 data-bind="visible: totalSurcharge() > 0">
    Total surcharge: $<span data-bind="text: totalSurcharge().toFixed(2)"></span>
</h3>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Passenger name</th><th>Meal</th><th>Surcharge</th><th></th>
        </tr>
    </thead>
    
    <tbody data-bind="foreach: seats">
        <tr>
            <td><input data-bind="value: name" /></td>
            <td><select data-bind="options: $root.availableMeals, value: meal, optionsText: 'mealName'"></select></td>
            <td data-bind="text: formattedPrice"></td>
            <td><a href="#" data-bind="click: $root.removeSeat">Remove</a></td>

        </tr>    
    </tbody>
</table>

<button data-bind="click: addSeat, enable: seats().length < 5">Reserve another seat</button>


